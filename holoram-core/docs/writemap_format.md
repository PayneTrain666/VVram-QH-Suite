# WriteMap Format

Every logical object stored in the substrate must have a WriteMap record containing:

- logical id
- sub id or tile/page id
- slot id
- channel base
- codec mode
- quality mode
- K
- generation
- CRC/integrity metadata when required
